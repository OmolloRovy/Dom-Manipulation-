

addButton.addEventListener("click", function() {
    if (newItem.value !== "") {
        ;
        });   
        li.innerText = newItem.value;
        li.appendChild(deleteButton);
       
    }
});
